# ── Secrets (NEVER commit API keys, tokens, passwords) ──
.env
auth.json
profiles/**/.env
profiles/**/auth.json
profiles/**/auth.lock
profiles/**/gateway.lock
profiles/**/gateway.pid
profiles/**/gateway_state.json
profiles/**/state.db*
profiles/**/logs/
profiles/**/cache/
profiles/**/sessions/
profiles/**/audio_cache/
profiles/**/image_cache/
profiles/**/sandboxes/
profiles/**/workspace/
profiles/**/plans/
profiles/**/platforms/
profiles/**/pairing/
profiles/**/home/
profiles/**/hooks/
profiles/**/cron/
profiles/**/skins/
profiles/**/models_dev_cache.json
profiles/**/.restart_last_processed.json
profiles/**/.skills_prompt_snapshot.json
profiles/**/channel_directory.json
profiles/**/gateway-service/
profiles/**/skills/
profiles/**/memories/*.lock

# ── Runtime state & ephemera ──
logs/
cache/
sessions/
cron/ticker_heartbeat
cron/ticker_last_success
cron/.jobs.lock
cron/.tick.lock
cron/executions.db
cron/output/
state.db
state.db-shm
state.db-wal
projects.db
verification_evidence.db
auth.lock
.mcp-discovery.lock
*.bak
kanban.db*
kanban/
context_length_cache.yaml

# ── Caches (model metadata, provider lists) ──
models_dev_cache.json
ollama_cloud_models_cache.json
provider_models_cache.json
.update_check
.update_exit_code
.skills_prompt_snapshot.json

# ── LSP server binaries & deps ──
lsp/

# ── Python/JS runtime binaries ──
bin/
hermes-setup.exe

# ── Memory system (runtime state, not config) ──
memories/MEMORY.md.lock
memories/USER.md.lock

# ── Empty/unused runtime dirs ──
audio_cache/
image_cache/
hooks/
pairing/
pets/

# ── Shared auth runtime ──
shared/

# ── State snapshots ──
state-snapshots/

# ── Process tracking ──
processes.json

# ── Skills (runtime-deployed) ──
skills/

# ── Bootstrap cache ──
bootstrap-cache/
